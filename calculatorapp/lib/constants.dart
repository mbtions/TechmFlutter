// Color constants
import 'package:flutter/material.dart';

// Color for gradient background
const Color gradientColor1 = Colors.blue;
const Color gradientColor2 = Colors.purple;

// Color for simple buttons with numbers
const Color numbersBgColor = Color.fromARGB(255, 167, 200, 250);

// Color for the operator buttons
const Color specialColor = Color.fromARGB(255, 247, 244, 244);

// Color for the answer buttons (=)
const Color answerBgColor = Color.fromARGB(255, 33, 150, 243);

// Color for the special buttons (AC, DEL, +/-, =) background
Color specialBgColor = Colors.purple;

// color for operator buttons background
const Color operatorsBgColor = Color.fromARGB(255, 241, 178, 252);
